# isrServ Discord Bot

Simple Discord bot for isrServ.

## Commands

- `!ping` – test if bot is alive
- `!status` – check bot status
- `!help` – list commands

## Environment Variables

- `TOKEN` – Discord bot token

## Local run

```bash
npm install
TOKEN=YOUR_DISCORD_BOT_TOKEN npm start
